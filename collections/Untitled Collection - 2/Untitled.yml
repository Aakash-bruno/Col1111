info:
  name: Untitled
  type: http
  seq: 1

http:
  method: GET
  url: google.com
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: example
    request:
      url: google.com
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: json
        data: "{}"
